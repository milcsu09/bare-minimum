
printf(fmt) : (*U8, ...) -> Void;

alias
  [
    x : F64,
    y : F64
  ] as Vec2;

vec2_create(x, y) : (F64, F64) -> Vec2 =
  {
    [x, y] # Auto cast to Vec2
  };

__main(argc, argv) : (I32, **U8) -> Void =
  {
    v := vec2_create (10, 20);
    printf("%f, %f\n", v.x, v.y);
  };

